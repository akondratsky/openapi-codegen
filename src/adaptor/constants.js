export const generatorVersion = '6.6.6';

export const schemaProperties = [
  'format',
  'minimum',
  'maximum',
  'exclusiveMinimum',
  'exclusiveMaximum',
  'minLength',
  'maxLength',
  'multipleOf',
  'minItems',
  'maxItems',
  'uniqueItems',
  'minProperties',
  'maxProperties',
  'additionalProperties',
  'pattern',
  'enum',
  'default'
];

// used by helper functions / convertToArray's toString method
export const arrayMode = 'length';
